{
  "name": "uniprot-website-e2e",
  "private": true,
  "description": "Browser tests for the UniProt website. A workspace package, so that it installs with the app and shares its Playwright version, but with its own scripts: run by hand, occasionally, and never part of the app's test run.",
  "scripts": {
    "test": "playwright test",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "25.9.0"
  }
}
