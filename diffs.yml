diffs:

from ARBA00013665, ARBA00013675, UR000048432, and UR000189784

Enum issues:
Information['dataClass']:
 - in schema: enum of all uppercase
 - in payload: enum with different casing
ex: "DOMAIN" vs "Domain"

Keyword['category']:
 - in schema: enum of all uppercase
 - in payload: enum with different casing
ex: "UNKNOWN" vs "Unknown"

Comment:
 - in schema: enum of all uppercase with underscores
 - in payload: enum of all uppercase with spaces
ex: "SUBCELLULAR_LOCATION" vs "SUBCELLULAR LOCATION"

SamTrigger['samTriggerType']:
 - in schema: enum of all uppercase
 - in payload: enum off all lowercase
ex: "TRANSMEMBRANE" vs "transmembrane"


Field name mismatches:
Condition:
 - in schema: 'negative'
 - in payload: 'isNegative'


Field type mismatches:
RuleException['accessions']:
 - in schema: Array<UniProtKBAccession>
 - in payload: Array<string>

Information['uniProtAccessions']:
 - in schema: Array<UniProtKBAccession>
 - in payload: Array<string>

UniProtKBCrossReference['database']:
 - in schema: UniProtKBDatabase
 - in payload: string


Missing fields in schema:
Comment:
 - in schema: no 'texts' field
 - in payload: 'texts' is Array<{value: string}>

Annotation & RuleExceptionAnnotation:
 - in schema: no 'proteinDescription' field
 - in payload: 'proteinDescription' is ProteinDescription

Annotation:
 - in schema: no 'subcellularLocations' field
 - in payload: 'subcellularLocations' is Array<{ location?: { value: string }; topology?: { value: string } }>
